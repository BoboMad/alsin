import { RegisterForm } from './register-form';

export default function RegisterPage() {
  return (
    <div className='flex items-center justify-center h-full p-2 w-full min-h-screen bg-gray-100'>
      <RegisterForm />
    </div>
  );
}
